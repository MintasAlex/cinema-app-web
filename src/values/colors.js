import { createTheme, ThemeProvider } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
      primary: {
        main: '#4B106C',
      },
      secondary: {
        main: '#f5f5f5',
      },
      tertiary: {
        main: '#FFFFFF',
      }
    },
  });
  